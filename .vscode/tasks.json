// filepath: /workspaces/devcontainer-go/.vscode/tasks.json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Delve",
            "type": "shell",
            "command": "dlv debug --headless --listen=:2345 --api-version=2 --accept-multiclient",
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}